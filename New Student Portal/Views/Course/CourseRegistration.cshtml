@model New_Student_Portal.ViewModel.StudentCourseDetails
@{
    // ViewBag.Title = "Course Registration";
    Layout = "~/Views/Shared/SiteMaster.cshtml";
}
<div class="card">
    <div class="card-body">
        <div id="divCourseReg"></div>
    </div> <!-- end card-body -->
</div> <!-- end card-->
@*//////////////////////Modal////////////////////////////*@
<div id="myModalUnits" class="modal fade" tabindex="-1" role="dialog" aria-hidden="true" style="display: none;">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title"><label id="labelT"></label></h4>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body p-4" id="modalBody">
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary waves-effect" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>
@*//////////////////////Modal////////////////////////////*@
<div id="myModalReports" class="modal fade" tabindex="-1" role="dialog" aria-hidden="true" style="display: none;">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title"><label id="labelTitle"></label></h4>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body p-4" id="modalBodyReport">
                <iframe id="iframe" style="width:100%;height:450px"></iframe>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary waves-effect" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>
<script type="text/javascript">
    $(document).ready(function () {
        LoadCourseRegistrationDetails();
    });
    function LoadCourseRegistrationDetails() {
        ShowProgress();
        $.ajax({
            async: true,
            type: "POST",
            datatype: "json",
            contentType: "application/json; charset = utf-8",
            processData: false,
            data: JSON.stringify({ T: `@Model.RegT` }),
            url: '@Url.Action("CourseRegistrationDetails", "Course")',
            success: function (data) {
                $("#divCourseReg").html(data);
                HideProgress();
            },
            error: function () {
                HideProgress();
                Swal.fire('Warning', 'There is some problem to process your request. Please try after some time', 'warning');
            }
        });
    }
    var ViewUnits = function (RegID, prog, stage, sem, regFor) {
        var x = document.getElementById("labelT");
        x.innerHTML = 'Student Units';
        var StudentUnits = {};
        StudentUnits.Programme = prog;
        StudentUnits.Stage = stage;
        StudentUnits.Semester = sem;
        StudentUnits.RegFor = regFor;
        StudentUnits.RegID = RegID;
        ShowProgress();
        $.ajax({
            url: "/Course/ViewStudentRegisteredUnits",
            datatype: "json",
            type: "POST",
            data: JSON.stringify({ Filters: StudentUnits }),
            contentType: "application/json; charset = utf-8",
            success: function (data) {
                HideProgress();
                $("#modalBody").html(data);
                $("#myModalUnits").modal("show");
            },
            error: function () {
                HideProgress();
                Swal.fire('Warning', 'Problem encountered while reading Units', 'warning');
            }
        });
    }
    var GetUnits = function (Prog) {
        $("#labelT").text("Units Registration Form");
        var regType = $("#RegType").children("option:selected").val();
        var Stage = $("#ddlStage").children("option:selected").val();
        if (regType == "") {
            Swal.fire('Warning', 'Select Registration Type', 'warning');
            return;
        }
        else if ((regType == "2") && (Stage == "")) {
            Swal.fire('Warning', 'Select Stage', 'warning');
            return;
        }
        else {
            ShowProgress();
            $.ajax({
                url: "/Course/GetProgramUnits",
                datatype: "json",
                type: "POST",
                data: JSON.stringify({ RegT: regType, Prog: Prog, Stage: Stage }),
                contentType: "application/json; charset = utf-8",
                success: function (data) {
                    HideProgress();
                    LoadCourseRegistrationDetails();
                    $("#modalBody").html(data);
                    $("#myModalUnits").modal("show");
                    HideProgress();
                },
                error: function () {
                    HideProgress();
                    Swal.fire('Warning', 'Problem encountered while reading Units', 'warning');
                }
            });
        }
    }
    var GetFreeElectiveUnits = function () {
        ShowProgress();
        var x = document.getElementById("labelT");
        x.innerHTML = 'Free Elective Course Registration';

        $.ajax({
            url: "/Course/GetFreeElectiveUnits",
            datatype: "json",
            type: "POST",
            //data: JSON.stringify({ Type: regType, HostelCode: HostelCode }),
            contentType: "application/json; charset = utf-8",
            success: function (data) {
                $("#modalBody").html(data);
                $("#myModalUnits").modal("show");
                HideProgress();
            },
            error: function () {
                HideProgress();
                Swal.fire('Warning', 'Problem encountered while reading Units', 'warning');
            }
        });
    }
    var SubmitSelectedUnits = function (RegT, Stage) {
        var UnitReg = new Array();
        $("#tblProgUnit tbody tr").each(function () {
            var row = $(this);
            if (row.find('input[type="checkbox"]').is(':checked')) {
                var Unit = {};
                Unit.UnitCode = row.find("td").eq(1).html();
                Unit.ClassCode = "";
                Unit.Day = row.find("td").eq(4).html();
                Unit.Period = row.find("td").eq(5).html();
                Unit.Campus = row.find("td").eq(6).html();
                UnitReg.push(Unit);
            }
        });
        if (UnitReg.length < 1) {
            Swal.fire('Warning', 'You have not selected any unit', 'warning');
            return;
        }
        else {
            ShowProgress();
            $.ajax({
                type: "POST",
                url: "/Course/SaveSelectedUnits",
                data: JSON.stringify({ UnitReg: UnitReg, RegT: RegT, Stage: Stage }),
                contentType: "application/json; charset=utf-8",
                dataType: "json",
                success: function (data) {
                    if (data.success == true) {
                        HideProgress();
                        $("#myModalUnits").modal("hide");
                        LoadCourseRegistrationDetails();
                        Swal.fire('Success', data.message, 'success');
                    }
                    else {
                        HideProgress();
                        $("#myModalUnits").modal("hide");
                        LoadCourseRegistrationDetails();
                        Swal.fire('Warning', data.message, 'warning');
                    }
                },
                error: function () {
                    HideProgress();
                    Swal.fire('Error', data.message, 'error');
                }
            });
        }
    }
    var DropUnit = function (prog, stage, sem, Unit) {
        var UnitFilters = {};
        UnitFilters.prog = prog;
        UnitFilters.stage = stage;
        UnitFilters.sem = sem;
        UnitFilters.Unit = Unit;
        ShowProgress();
        $.ajax({
            url: "/Course/DropBasketUnit",
            datatype: "json",
            type: "POST",
            data: JSON.stringify({ Filters: UnitFilters }),
            contentType: "application/json; charset = utf-8",
            success: function (data) {
                if (data.success == true) {
                    HideProgress();
                    LoadCourseRegistrationDetails();
                    Swal.fire('Success', data.message, 'success');
                }
                else {
                    HideProgress();
                    Swal.fire('Warning', data.message, 'warning');
                }
            },
            error: function () {
                HideProgress();
                Swal.fire('Error', data.message, 'error');
            }
        });
    }
    var RegisterSelectedUnits = function () {
        var totalRowCount = $("#tblUnitBasket>tbody>tr.rowUnits").length;
        if (totalRowCount < 1) {
            Swal.fire('Warning', 'There are no units selected to submit!!', 'warning');
            return;
        }
        else {
            ShowProgress();
            $.ajax({
                type: "POST",
                url: "/Course/RegisterSelectedUnits",
                contentType: "application/json; charset=utf-8",
                dataType: "json",
                success: function (data) {
                    if (data.success == true) {
                        if (data.Redirect == true) {
                            window.location = data.message;
                        }
                        else {
                            HideProgress();
                            LoadCourseRegistrationDetails();
                            Swal.fire('Success', data.message, 'success');
                        }
                    }
                    else {
                        HideProgress();
                        LoadCourseRegistrationDetails();
                        Swal.fire('Error', data.message, 'error');
                    }
                },
                error: function (data) {
                    HideProgress();
                    Swal.fire('Error', data.message, 'error');
                }
            });
        }
    }
    var DropRegisteredUnit = function (button, prog, sem, Unit, UnitName, RegFor) {
        Swal.fire({
            title: 'Are you sure?',
            text: "You won't be able to revert this!",
            icon: 'warning',
            showCancelButton: true,
            confirmButtonText: 'Yes, delete it!',
            cancelButtonText: 'No, cancel!',
            confirmButtonClass: 'btn btn-success mt-2',
            cancelButtonClass: 'btn btn-danger ms-2 mt-2',
            buttonsStyling: false
        }).then(function (result) {
            if (result.value) {
                var UnitFilters = {};
                UnitFilters.prog = prog;
                //UnitFilters.stage = stage;
                UnitFilters.sem = sem;
                UnitFilters.Unit = Unit;
                UnitFilters.UnitName = UnitName;
                UnitFilters.RegFor = RegFor;
                ShowProgress();
                $.ajax({
                    url: "/Course/DropRegisteredUnit",
                    datatype: "json",
                    type: "POST",
                    data: JSON.stringify({ Filters: UnitFilters }),
                    contentType: "application/json; charset = utf-8",
                    success: function (data) {
                        if (data.success == true) {
                            HideProgress();
                            Swal.fire('Success', data.message, 'success');
                            LoadCourseRegistrationDetails();

                            var row = $(button).closest("TR");
                            var table = $("#tbRegUnits")[0];
                            //Delete the Table row using it's Index.
                            table.deleteRow(row[0].rowIndex);
                        }
                        else {
                            HideProgress();
                            Swal.fire('Warning', data.message, 'warning');
                        }
                    },
                    error: function () {
                        HideProgress();
                        Swal.fire('Error', data.message, 'error');
                    }
                });
            } else if (result.dismiss === Swal.DismissReason.cancel) {
                Swal.fire({
                    title: 'Cancelled',
                    text: 'Unit has not been dropped. Its safe',
                    icon: 'error',
                    confirmButtonColor: "#1abc9c",
                });
            }
        });
    }
    var PrintCourseStatement = function () {
        var x = document.getElementById("labelTitle");
        x.innerHTML = 'Course Statement';
        ShowProgress();
        $.ajax({
            url: '/ViewDocuments/PrintCourseStatement',
            type: "POST",
            //async: false,
            success: function (data) {
                if (data.success == true) {
                    $('iframe').attr('src', data.message);
                    $("#myModalReports").modal("show");
                    HideProgress();
                }
                else {
                    Swal.fire('Warning', data.message, 'warning');
                    HideProgress();
                }

            },
            error: function (err) {
                HideProgress();
                Swal.fire('Warning', err, 'warning');
            }
        });
    }
    var GetProformaInvoice = function () {
        var x = document.getElementById("labelTitle");
        x.innerHTML = 'Proforma Invoice';
        ShowProgress();
        $.ajax({
            url: '/ViewDocuments/ProformaInvoice',
            type: "POST",
            //async: false,
            success: function (data) {
                if (data.success == true) {
                    $('iframe').attr('src', "data:application/pdf;base64," + data.message);
                    $("#myModalReports").modal("show");
                    HideProgress();
                }
                else {
                    Swal.fire('Warning', data.message, 'warning');
                    HideProgress();
                }
            },
            error: function (err) {
                HideProgress();
                Swal.fire('Warning', err, 'warning');
            }
        });
    }
</script>

