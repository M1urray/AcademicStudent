@model New_Student_Portal.ViewModel.GetRegisteredUnit
@using System.Globalization;
@{
    bool droUnit = false;
    if (Model.RegDeadlineDate != "")
    {
        DateTime regDeadLine = DateTime.ParseExact(Model.RegDeadlineDate.Replace("-", "/"), "dd/MM/yyyy", CultureInfo.InvariantCulture);
        if (regDeadLine >= DateTime.Today)
        {
            droUnit = true;
        }
        else
        {
            droUnit = false;
        }
    }
}
<style>
    .BtnPadding {
        padding: 1px;
        font-size: 12px;
    }
</style>
<div class="responsive-table-plugin">
    <div class="table-rep-plugin">
        <div class="table-responsive" data-pattern="priority-columns">
            <table id="tbRegUnits" class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th>
                            Unit
                        </th>
                        <th>
                            Description
                        </th>
                        <th style="text-align:center">
                            Section
                        </th>
                        <th style="text-align:center">
                            CH
                        </th>
                        @if (droUnit)
                        {
                            <th style="text-align:center">
                                Action
                            </th>
                        }
                    </tr>
                </thead>
                <tbody>
                    @{
                        if (Model.ListOfRegUnit != null)
                        {
                            if (Model.ListOfRegUnit.Count() > 0)
                            {
                                foreach (var item in Model.ListOfRegUnit)
                                {
                                    <tr>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Unit)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Unit_Name)
                                        </td>
                                        <td align="center">
                                            @Html.DisplayFor(modelItem => item.Section)
                                        </td>
                                        <td align="center">
                                            @Html.DisplayFor(modelItem => item.CF)
                                        </td>
                                        @if (droUnit)
                                        {
                                            <td align="center">
                                                <button class="btn btn-warning BtnPadding" onclick="DropRegisteredUnit(this,'@item.Programme','@item.Semester','@item.Unit','@item.Unit_Name','@item.RegFor')">Drop</button>
                                            </td>
                                        }
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr>
                                    <td colspan="5" style="color:red">No Units Found</td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="5" style="color:red">@ViewBag.Error</td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>


