@model IEnumerable<New_Student_Portal.ViewModel.Notifications>
<div class="row">
    <div class="col-md-12">
        @{
            if (Model != null && Model.Count() > 0)
            {
                foreach (var item in Model)
                {
                    <div>
                        @item.Message
                        <hr />
                    </div>
                }
            }
            else
            {
                <div>
                    No Records Found
                </div>
            }
        }
    </div>   
</div>

